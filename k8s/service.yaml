apiVersion: v1
kind: Service
metadata:
  name: quickspin-frontend
  namespace: quickspin-frontend
  labels:
    app: quickspin-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: quickspin-frontend
